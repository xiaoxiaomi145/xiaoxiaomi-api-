version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=your_super_secret_jwt_key
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./database.sqlite:/app/server/database.sqlite
    restart: always
